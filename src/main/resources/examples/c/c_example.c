int main()
{
    int n = 0;
    statement(s);
  if( a < 20 ) {
          printf("a is less than 20\n" );
       }
        else {
          printf("a is less than 20\n" );
       }
        statement(s);
}