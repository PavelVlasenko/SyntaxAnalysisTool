int main()
{
    int n = 0;
    statement(s);
      for ( init; condition; increment ) {
             for ( init; condition; increment ) {
                  statement(s);
               }
        }

    statement(s);
}