int main()
{
    int n = 0;
    statement(s);

  if( a < 20 ) {
         for ( init; condition; increment ) {
             if( a < 20 ) {
                  continue;
                 }
             }
           }
 statement(s);
}